[project]
name = "llm-guardrail"
version = "0.1.0"
description = "A tiny LLM guardrail library for Terraform provider simulation."
requires-python = ">=3.9"
dependencies = []

[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"
