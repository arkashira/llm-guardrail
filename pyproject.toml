[project]
name = "llm-guardrail"
version = "0.1.0"
description = "In‑memory LLM output guardrail with a simple live dashboard."
authors = [{name = "Axentx"}]
requires-python = ">=3.9"
dependencies = []

[tool.pytest.ini_options]
testpaths = ["tests"]
